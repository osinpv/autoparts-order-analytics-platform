apiVersion: v1
kind: Service
metadata:
  name: autoparts-api
  namespace: autoparts
spec:
  type: NodePort
  selector:
    app: autoparts-api
  ports:
    - port: 8000
      targetPort: 8000
      nodePort: 30080